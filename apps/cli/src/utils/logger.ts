/**
 * Logger utility for the CLI
 */
import { Logger } from '@obsidian-magic/core/utils/Logger';

/**
 * Shared logger instance for the CLI application
 */
export const logger = Logger.getInstance();
